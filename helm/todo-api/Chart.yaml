apiVersion: v2
name: todo-api
description: Helm chart for Todo API Spring Boot application
type: application
version: 0.1.0
appVersion: "2.0"  # Updated to match container image version
dependencies:
  - name: mongodb
    version: "0.14.2"
    repository: "oci://registry-1.docker.io/cloudpirates"
    condition: mongodb.enabled
